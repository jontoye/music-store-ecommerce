export type NavLink = {
  title: string;
  sublinks?: string[];
};

export type ImgUrl = {
  id: number;
  url: string;
};

export type Category = {
  name: string;
  image: string;
  slug: string;
};
